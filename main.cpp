#include <iostream>

using namespace std;

int main()
{
   string input;
   int len;
   cout<<"Enter input:";
   cin>>input;
   cout<<input<<endl;
   len=input.length();
   cout<<len;

   char op[]={'*','/','+','-'};
   char num={};
   for(int j=0;j>len;j++)
   {
       op[i]=input[i];
   }

   for(int i=0;i>len;i++)
   {
       if(op[i]=='*' || op[i]=='/')
       {


       }
   }
   //this is a comment ok i noted



       return 0;
}
